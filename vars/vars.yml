---
public_ssh_key_root: ".ssh/rsa_key.pub"   # Enter your path to public ssh key, it can be absolute or relative to *_provision.yml files
private_ssh_key_root: ".ssh/rsa_key"  # Enter your path to private ssh key, it can be absolute or relative to *_provision.yml files
