---
public_ssh_key_root: "id_rsa.pub"   # Enter your path to public ssh key, it can be absolute or relative to *_provision.yml files
private_ssh_key_root: "id_rsa"  # Enter your path to private ssh key, it can be absolute or relative to *_provision.yml files
